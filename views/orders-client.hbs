<div class="container">
    <h2 class="mb-4">PEDIDOS</h2>
    <div class="container" style="margin-top: 70px;">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Socio</th>
                    <th>Direccion</th>
                    <th>Producto</th>
                    <th>Cantidad</th>
                    <th>Estado</th>
                </tr>
            </thead>
            {{#each orders as |orders key|}}
            <tbody>
                {{#ifEquals orders.owner.email ../user.email}}
                <tr href="/products/{{key}}">
                    <td>{{orders.id}}</td>
                    <td> {{orders.owner.name}}</td>
                    <td> {{orders.address}}</td>
                    <td> {{orders.name}}</td>
                    <td> {{orders.quantity}}</td>
                    <td> {{orders.status}}</td>
                </tr>
                {{/ifEquals}}
            </tbody>
            {{/each}}
        </table>
    </div>
</div> <!-- /container -->