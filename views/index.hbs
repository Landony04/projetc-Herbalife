<div class="container">
    <!-- Example row of columns -->
    <h2 class="mb-4">Últimas preguntas</h2>
    {{#each products as |products key|}}
    <div class="card bg-white mb-3">
        <div class="card-body">
            <div class="row">
                <div class="col-2 text-center">
                    <a href="#" class="text-dark">
                        <h3>
                            2
                        </h3>
                        Respuestas
                    </a>
                </div>
                <div class="col-10 d-flex align-items-stretch">
                    <h4 class="mt-3">
                        <a href="/products/{{key}}" class="text-dark">{{products.name}}</a>
                    </h4>
                </div>
            </div>
        </div>
    </div>
    <hr>
    {{/each}}
</div> <!-- /container -->